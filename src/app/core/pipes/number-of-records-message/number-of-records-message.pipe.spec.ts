import { NumberOfRecordsMessagePipe } from './number-of-records-message.pipe';

describe('NumberOfRecordsMessagePipe', () => {
  it('create an instance', () => {
    const pipe = new NumberOfRecordsMessagePipe();
    expect(pipe).toBeTruthy();
  });
});
